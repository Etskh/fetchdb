'use strict';

const expect = require('expect');
/*
describe('lib / connectors / memory', function() {

  const fetchdb = require('../lib/fetchdb');
  const modelName = 'model';
  const model = fetchdb(modelName);

  describe('#', function() {
    it('will create a connector with ', function(done) {

      // create a new connector and model without config
      model.info().then( (info) => {
        expect(info).toEqual({
          type: 'memdb',
          name: modelName,
          count: 0,
        });
        done();
      });
    });
  });
});
*/
